package com.example.androiddesignpatterns.patternsCategories.creational.singleton

/*
* There are cases when you need only one instance of a class.
* So, whenever you call the object of the class, then the new object should
* not be created(only one-time object creation will be there).
* This design pattern provides a global point of access to a class.
* */

object Singleton {

    fun doSomething() {

    }
}